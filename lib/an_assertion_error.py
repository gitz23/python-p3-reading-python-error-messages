#!/usr/bin/env python3

# An assert() statement tells the interpreter that the code inside of it must proceed 
# without error or exception. If an assertion fails, an AssertionError is raised.

assert(1 == 1)
